// Placeholder for future backend logic

document.querySelectorAll(".action").forEach(btn => {
  btn.addEventListener("click", () => {
    alert("Feature will be available after login.");
    window.location.href = "auth.html";
  });
});
